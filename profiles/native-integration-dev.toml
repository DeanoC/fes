host_os = "linux"
host_arch = "amd64"
image_variant = "native-dev"
version = "0.1.0"
native_image_mode = "package-only"
check_packages = true

# Current component revisions are the parent gitlinks. Historical profiles
# explicitly select earlier commits; this profile integrates this ordered set.
[[fpga_packages]]
core_id = "fes.pong"

[[fpga_packages]]
core_id = "fes.zx81"

[[fpga_packages]]
core_id = "fes.coleco"
