host_os = "linux"
host_arch = "amd64"
image_variant = "native-dev"
version = "0.1.0"
fpga_source = "misteross"
fpga_cores = ["megadrive", "pong", "snes"]
quartus_version = "17.0.2"
bundle_interface = "selection"
check_packages = true
# Current component revisions are the parent gitlinks. Historical profiles
# explicitly select earlier commits; this profile integrates the current set.
