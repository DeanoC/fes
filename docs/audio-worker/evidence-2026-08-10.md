# FogCast audio-worker evidence — 2026-08-10

## Scope and classification

This is separate product-audio evidence. It does not modify the accepted Stage
A technical scope and does not advance a roadmap stage to Accepted. The
historical Stage A records remain authoritative, including the audio-path
limitation, capture follow-up, and HIL observation.

Current classification: **Software-tested** for host composition, source
ownership, authenticated PCM transport, and the retained-testbed diagnostic
bridge. Two repeated named ShadowCast captures were non-zero under the
authorized Terminal responsible-process context, as recorded below. They are
machine observations within this Software-tested record, not the roadmap's
formal Reproducible evidence state. The FogCast audio worker and its target
transport remain separate end-to-end gates.
**HIL-observed** and **Accepted** are not claimed: the disposable target
currently reports only the historical Dummy audio route, and no
coordinator-owned non-Dummy AudioSink or physical HDMI/analyzer result is
evidenced here.

## Provenance

This table identifies the audio-worker implementation provenance. The later
host-capture observations have their own bounded host/device conditions and
private artifact hashes; they are not a clean rebuild of this branch.

| Field | Value |
| --- | --- |
| Repository base | `95a0a73997a65cf5232dfbe4965a9f6a0e3c6340` |
| Working branch | `codex/fogcast-audio-worker` |
| Host source | `shadowcast_uac` or `host_output` (operator-selected per run) |
| Endpoint identity | `sha256:<recorded-in-private-receipt>`; do not copy the UID into this file |
| Format | PCM16LE, 48,000 Hz, 1–2 channels, 240 samples/frame |
| Worker identity | Distinct video/audio SSRCs; same session and generation |
| Transport | Separate video/audio RTP and control routes; exact addresses remain private |
| Target sink | No non-Dummy route evidenced; bridge sink is null or private PCM dump only |

The 2026-08-11 end-to-end attempt used the enumerated `ShadowCast 3` audio
endpoint (`[0]` in the current AVFoundation list), the recorded endpoint
digest `sha256:a99e3bd9625583ce8139a62bc8cbf9b2a96177e0f4f241b03fd69777b03226dd`,
the current signed helper CDHash `aad0d8f0b9c8a48bb1e5c2e45c751cf67635a0e6`,
and the ARMv7 diagnostic bridge hash
`sha256:6aef001720f8ad257eec49b8fdd4cbcdefcd8ac66f6db26e0fe286936208c157`.
The Mac speaker was muted during the attempt and restored afterward. No
transport counters are reported for that run: the helper returned
`Microphone authorization is denied` before `MEDIA_HELLO`, and the target
bridge therefore ended with `packets=0 frames=0 non_zero_samples=0`. This is a
permission/admission failure, not evidence that Sonic was silent; Genki was
left running as the concurrent viewer ground truth.

## Repeated named raw-PCM comparator — 2026-08-11

This follow-up deliberately separates the raw-device question from FogCast's
video-first media composition and from target audio playout. The operator
confirmed **Sonic 2** visibly loaded in Genki after the disposable target's
cached Mega Drive content was selected with an explicit MGL path relative to
the registered games root. Machine state concurrently reported
`CORENAME=MegaDrive`; the cached Sonic 2 object retained SHA-256
`193bc4064ce0daf27ea9e908ed246d87ec576cc294833badebb590b6ad8e8f6b`.
This is target/game fixture context, not an audio payload claim.

The sole AVFoundation/CoreAudio input remained exact-name `ShadowCast 3`
(AVFoundation audio index `[0]`), USB, two input channels, 48,000 Hz, and the
recorded stable endpoint digest
`sha256:a99e3bd9625583ce8139a62bc8cbf9b2a96177e0f4f241b03fd69777b03226dd`.
Mac mini Speakers were explicitly muted throughout. With Genki live, the
Codex-shell direct comparators both produced valid-length exact-zero PCM:

| Selection | Format | Samples | Non-zero samples | Peak | SHA-256 |
| --- | --- | ---: | ---: | ---: | --- |
| `none:ShadowCast 3` | PCM16LE, 48 kHz, stereo | 452,608 | 0 | 0 | `cfc02e3da3fcd957269a7d662e880bb05601b2ba87829a12f041335d30438ff4` |
| `none:0` | PCM16LE, 48 kHz, stereo | 448,000 | 0 | 0 | `a82b89a19642c25a64180ca250d2a76ee9bb0aafd0edbba2dad6efb4e00c077c` |

The viewer was then released, without changing the selected endpoint, target
run, mute state, or audio format. The exact-name command was launched by
`com.apple.Terminal` (rather than the Codex app-server shell) twice:

```text
ffmpeg -nostdin -hide_banner -loglevel error -f avfoundation \
  -i 'none:ShadowCast 3' -t 8 -vn -ac 2 -ar 48000 -f s16le <private-output>
```

| Run | Bytes | Samples | Non-zero samples | Peak | RMS | SHA-256 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| Terminal run 1 | 1,191,936 | 595,968 | 302,413 | 8,013 | 1163.391 | `1235bbeb5ade607aa6dff0e5bf794ec730a97d0d7bcd3deefefb9e797224b9ea` |
| Terminal run 2 | 1,191,936 | 595,968 | 190,654 | 10,364 | 1126.770 | `d76ca66ba2c776387dcb000d3fa419b94837197223f074a2715e0bed0b42a83e` |

Genki was reopened and its start control activated after each bounded
alternate-ownership run. These two output files remain private temporary
artifacts; the published hashes, exact endpoint binding, format, and metrics
are the deterministic record. The result satisfies the raw named-endpoint
criterion: ShadowCast's UAC payload was non-zero in two repeated Sonic 2
captures under the Terminal responsible-process context, even with Mac speaker
output muted. It does not show that a Codex-shell FFmpeg process, the FogCast
helper, or the target bridge received those samples.

The strongest remaining hypothesis is therefore a macOS
responsible-process/TCC/AVFoundation admission distinction, not a silent
MiSTer source and not an `none:0` index-selection error. The exact next
experiment is to run the already signed `com.fogcast.host` helper with the
same named/hash-bound source and a fresh authenticated video receiver plus
retained-testbed audio bridge, while Genki is released. Require positive
helper and bridge packet/frame/non-zero counters, then repeat with the Mac
speaker muted. A target AudioSink/HDMI-playout claim remains outside this
diagnostic bridge and Stage A's accepted scope.

## Machine-observed checks

The host composition opens an audio source only at launch, creates a fresh
source per launch, validates shared session/generation/token identity and
distinct routing/SSRCs, starts video before audio, rolls back video on audio
failure, and independently bounds cleanup. The bridge tests authenticate
`MEDIA_HELLO`, reject a wrong token, consume non-zero PCM16 RTP, retain a
token-file-only interface, and bound sink cleanup. ARMv7 cross-build and race
tests are required repository checks.

Record the exact final commands and results below without embedding private
paths or tokens:

```text
host helper hash: sha256:<redacted-local-artifact>
bridge hash: sha256:<redacted-local-artifact>
session/generation: <redacted run identifiers>
video SSRC: <redacted>
audio SSRC: <redacted>
packets: <count>
frames: <count>
non_zero_samples: <count>
speaker muted: yes/no
viewer concurrent: yes/no
```

## Operator observations and inference

Genki can render and play the active Sonic run, including while its own
application is authorized. That observation must be kept separate from raw
ShadowCast endpoint capture. The completed named/indexed comparison shows that
the Codex-shell context can remain exact-zero while the signed-Terminal
context receives non-zero PCM. The remaining product experiment is the
signed-helper/bridge run below; a known sound-capable HDMI sink or independent
analyzer is relevant only after that product source is non-zero and target
playback remains unverified.

## Unresolved FogCast end-to-end gate and exact next experiment

The earlier signed-helper run failed its Microphone preflight before
`MEDIA_HELLO`; the operator subsequently authorized the helper. That changed
grant has not yet been revalidated by a full FogCast worker run. Separately,
the product starts video before audio, so the end-to-end run needs an
authenticated video receiver and retained-testbed audio bridge ready during
the helper's bounded admission window. Those are product-integration gates,
not new evidence about the ShadowCast source.

The exact next experiment is:

1. Keep Sonic 2 active, release Genki for the initial signed-helper run, and
   verify the named endpoint digest above.
2. Start fresh private video-receiver and audio-bridge instances with one
   session/generation; use a null or private-dump bridge sink only.
3. Launch `com.fogcast.host` with the existing named/hash-bound UAC source;
   require successful Camera and Microphone preflight plus positive helper and
   bridge `packets`, `frames`, and `non_zero_samples` counters.
4. Repeat with Mac mini Speakers muted, then reopen Genki and perform the
   documented concurrent-ownership comparison. Do not make a target-playout
   or Stage A claim from the diagnostic bridge.

## Safety boundary

No target credentials, endpoint UIDs, certificates, signing identities, or
private target configuration belong in this record. The bridge is not a
coordinator, does not own `host_cast`, and must not be used as physical HDMI
evidence. The accepted Stage A evidence is preserved unchanged.
