# FES SG-1000 package-only kit launch/Stop HIL

Status: GREEN. This is package-only physical hardware-in-the-loop evidence
for `fes.sg1000`. It claims only sealed-package admission, one library launch,
and Stop lifecycle behavior on the designated kit. It does not claim media,
input, HDMI, controller, image, FAT, appliance, or gameplay acceptance.

## Scope and exact identities

The tested archive was the user-designated sealed artifact:

| Item | Value |
| --- | --- |
| FES worktree | `/Users/clawzai/Developer/fes-wt-sg1000-package-kit-hil` @ `29c184bd2ee0bb4a709e92676eca9b2ce861fdd3` |
| misteross producer | `bbbcef4c05b6e863dd89cf3ce65dfa0b8d085de8` (tip of #64) |
| Core | `fes.sg1000` |
| Package ID | `41b7250266b8284b43a34a42f55c8b6d150627078daddd76d8eb4218cd5182a7` |
| BUILD_ID | `e37c39bb57c788c905eca12ab746a8c3` |
| Archive | `/Users/clawzai/tmp/fes-sg1000-package/fes.sg1000.41b72502.fcore` |
| Archive SHA-256 | `0b5942ea730817a13b8f64e351fcc30834594dff6e7518d9152de613e7d86a02` |
| Archive bytes | `2532864` |
| Payload/RBF SHA-256 | `5d985b4c43e015b8d9a14cebdb393f4e6593b7a7546e39478f4a9d4b38193d5f` |
| Target ID | `73dc9f5f-1a12-4a95-a820-a9b4e600769a` |
| Target address | `http://mister.lan:8182` (`192.168.10.84`) |

The archive manifest reports format 2, core `fes.sg1000`, build ID
`e37c39bb57c788c905eca12ab746a8c3`, and producer revision
`bbbcef4c05b6e863dd89cf3ce65dfa0b8d085de8`.

## Temporary host and live platform

The isolated-container route was not used: this workstation is Darwin arm64
and the available local Docker images were build-stage images, not an existing
immutable FES host/boot-media runtime image. No image was pulled or built and
no host or component was compiled for this run.

A new headless temporary API was started on ephemeral loopback port `58374` at
`http://127.0.0.1:58374`, using private owner-only `HOME`/`TMPDIR` and a
private configuration with exactly one enabled target: the designated target
above. The host binary was the retained SMS-HIL platform binary
`/tmp/fes-sms-kit-hil/fogcast-api-2047`, SHA-256
`ea4b5807955b90e671a1aaed532157013f40371db90c4730cee41744ad20f11e`.

The preflight health response in
`/tmp/fes-sg1000-kit-hil/pre-acceptance-health.json` reported:

| Revision/identity | Observed value |
| --- | --- |
| Host revision | `2047f4c258e0ae5efb8eb0315a5225504181703d` |
| Agent revision | `2047f4c258e0ae5efb8eb0315a5225504181703d` |
| Runtime revision | `f700e342023e21f5917857326a0c533d621905b8` |
| Target boot ID | `fc3efd01-18b4-471c-90eb-b9d6f6d517b2` |
| Target state | `ready`; preflight session `idle` |

The long-lived `127.0.0.1:8787` API was not used as the acceptance path and
`fogcast-api.service` was not enabled or changed. The private temporary host
catalog/configuration was removed after shutdown; the receipt and non-secret
diagnostic JSON remain under `/tmp/fes-sg1000-kit-hil/`.

## Kit lease and cleanup

The initial read-only `kit.py status` was free at generation
`bd70201b8cbe434a24a4d1c7824beb6fda9f8ac40695877d78e21e651b953b0c`. A
run-specific reservation session for `Caster@ai-dev-mac` held generation
`ad6a21629308843e4b0a1a0cd988fb0cb863fadbf68253ab1d4c3f0feca7fb68` with
purpose `fes.sg1000 package HIL launch+Stop`, then received an explicit
`release`. The follow-up status was `free` at generation
`ef525ca121daa7778dc9250d2790e2bc4acfa34ce6772c722975d304517b3e18` before
the package runner began.

The package runner used the temporary host's application-owned lease for its
launch and Stop. Its receipt confirmed Stop to `idle`; after the temporary API
exited, a direct target status probe still reported `idle` and the final
independent `kit.py status` was `free` at generation
`becb0359885307a9a1a9bd18a589dc807f523943edcfa7c34805f7ccf7093752`.
The temporary API printed `fogcast-api: session cleanup failed` during process
shutdown after the successful runner Stop. No active target session or lease
remained, so no retry or unrelated Stop was attempted.

## Package-only acceptance

The runner was invoked with `--execute`, `--new-entry-title 'FES SG-1000 kit
HIL'`, the exact archive/package/core/target identities above, and the probed
host, agent, and runtime revisions. It passed with a new library entry
`fpga-fes-sg-1000-kit-hil-99241c6f6933`.

Receipt: `/tmp/fes-sg1000-kit-hil/receipt.json`

Receipt SHA-256:
`c24fb85de904aaa5b702d9e8d444816e07348ee64ac7b516be3415398726360f`

The receipt records `success=true`, `mode=lifecycle-only`, compatible package
admission, a new selection, and one owned session:

- session ID `9aa14566-fa46-4536-abd3-4af8adf4b3ef`;
- flight ID `3b220ecb-d77d-41f0-b0af-ef2856dd6dbd`;
- generation `2`; and
- the expected package and target IDs.

Stop confirmation reports the same session and flight, target state `idle`,
the expected target ID, `shutdown_reason=session_stop`, detached input, one
release, and an empty diagnostics list. No media, input, HDMI, or controller
operation was requested.

No Quartus, tip-image closed-set change, FES #61/FogCast #248 reopen, FogCast
allowlist change, FAT or `linux.img` rebuild, `.4`/spare `WRITE_GO`, direct
runtime/JTAG/Main programming, or persistent host-service enablement occurred.
No FES source, parent pin, image, or component checkout was changed; this
validation document is the only worktree change and remains uncommitted.
