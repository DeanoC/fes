STATUS=GREEN
HEAD=956e30fa4e51b1719706f9eaa9b996bec11496b8

Fixes on feat/native-tenfoot-launcher (PR https://github.com/DeanoC/FogCast/pull/93):

1) P1 covers: queueVisibleWork now evicts a.covers (decoded images and slots)
   outside Grid.PrefetchRange. applyResult drops out-of-window results.
   TestAppEvictsDecodedCoversOutsidePrefetchWindow passes.

2) P2 launch: startLaunchLocked calls launchBlockReason (Launchable / state /
   root_online, same checks as ui_shell). Blocked titles set launch phase
   error and do not POST /api/v1/session/launch.
   TestLaunchBlockReasonMirrorsWebUI and
   TestAppRejectsUnavailableTitlesWithoutHostLaunch pass.

3) P2 smoke: virtualPress no longer calls app.Press. If the SDL gamepad path
   does not apply the command, smoke fails. Virtual smoke now drives the
   opened gamepad only (no extra SDL_OpenJoystick). Live make tenfoot-smoke:
   focus 0->1, launch_blocked="This game's source is offline."

4) Bugbot: dummy video SetHint is SDL_VIDEO_DRIVER.

Mac: go test ./host/tenfoot, go test -tags sdl3 ./host/tenfoot,
make build-fogcast-tenfoot, make tenfoot-smoke all green. Kit / MiSTer untouched.
