---
schema: fogcast.stage-a0.main-lock-review.v1
candidate_lock_sha256: 415445d1871a25b6f6f9f30d52deec640147beea6a23d8527ddc5741f4ae789a
decision: blocked
decision_date: 2026-08-09
---

# Stage A0 candidate lock review

## Scope

This record reviews the candidate bytes of
`build/stage-a0-main.lock.toml` at FogCast commit `c834aba`. It is the
hash-named record required by the Stage A0 design; it does not promote or
rewrite the candidate lock.

The reviewed evidence class is **Software-tested / local-only**. The decision
is **blocked**. No Reproducible, HIL-observed, or Accepted claim is made.

## Commands and machine observations

The candidate generator was run against the reviewed adapter capture and
emitted all six canonical policy files plus `materials.json`:

```text
mise exec go@1.26.5 -- go run ./cmd/stage-a0-policy-candidate \
  --repository <pinned-main-fork> \
  --source-material main-fork \
  --authority <reviewed-authority.json> \
  --build-log <adapter-capture>/build.log \
  --receipt <adapter-capture>/first-build.json \
  --artifact-dir <adapter-capture> \
  --toolchain-archive <pinned-toolchain.tar.xz> \
  --toolchain-root <pinned-toolchain-root> \
  --output-dir <new-policy-candidate-directory>
```

Two fresh adapter captures compare as follows:

| Observation | Value |
| --- | --- |
| `bin/MiSTer` | `f9e6fd646740449186b74821a3684686d5dbc7b33e28052e9de28b8f4c751f2e` |
| `bin/MiSTer.elf` | `51a9864bb12ebdf8961b30ac0a45d533fd2a2885a96d81fc29f8363d1804706d` |
| inventory entries | `227` |
| inventory digest | `7a2e77ffa919e504a4baa638d6b820085a4464a15281449f1abc2f4d66419cd5` |
| left build-log digest | `179d2f2036e82549b5bf5e5bf5ed35e782e0fae9d2a55ea43609519d029a7297` |
| right build-log digest | `d123f062cb79cf6b90e41b93e88242f9c8f459315686b215dc13aae823eb8b34` |
| preliminary comparison | byte-identical retained final artifacts |

The promotion decision was generated with the canonical report command:

```text
bin/stage-a0-promotion-report \
  --lock build/stage-a0-main.lock.toml \
  --policy-dir <policy-candidate-directory> \
  --materials <policy-candidate-directory>/materials.json \
  --comparison <precompare-directory>/comparison.json \
  --output <new-promotion-report.json>
```

The v1 report is deliberately a blocked-only candidate decision; it cannot
claim eligibility or rewrite the lock. Its decision is `blocked` with these
stable blockers:

```text
LOCK_SCHEMA_INVALID
MATERIAL_CATALOG_NOT_PROMOTED
POLICY_PROMOTION_BLOCKED
SOURCE_NOT_DURABLY_RETRIEVABLE
```

The two-build comparison itself passes its preliminary byte-identity check;
its `local-only` classification prevents a Reproducible claim.

The retained canonical evidence hashes are:

| Evidence | SHA-256 |
| --- | --- |
| precompare report | `a1700202b382fb13a873e3dc23de6b643cab2cbeed8edc806336619bea35b927` |
| promotion report | `648d88a186a186e3b4ce96bf71443838a052577e2d3b5934b93d35f0d507a6d1` |

The report also binds the observed material authority and comparison source,
toolchain, container, and build-date fields to a valid lock before any future
promoted-evidence schema can consume them.

## Review and dispositions

| Finding | Disposition |
| --- | --- |
| Candidate lock contains empty policy material paths/hashes and blocked license IDs | Remains blocked; no values are inferred. |
| Candidate `materials.json` has unresolved build-log, utility/license, container, fork-retrieval, and material-license review | Remains blocked; local OCI identity is observation only. |
| ELF normalized readelf/objdump fields are Go projections, not pinned tool outputs | Remains an explicit final-policy requirement. |
| Pinned Overlord/resource checkouts lack the required DE10-Nano/Cyclone V definitions | Recorded in the separate Overlord slice handoff; no generated output is claimed. |

## Reviewer roles

- Root coordinator: integrated the candidate and report; no independent
  promotion approval.
- Vega: independent Stage A0 review using `gpt-5.6-sol`; no Critical findings
  on the candidate observer changes; the candidate remains non-promotable.
- Sol: architecture direction remains the approved ADR 0001 boundary; no
  architecture change is introduced by this record.

## Next safe action

Resolve the material/license and durable-retrieval records, then run the
promotion report again against a new hash-named lock candidate. Independently,
add the minimum Overlord DE10-Nano/Cyclone V resources and capture generated
memory-map, register, toolchain, and software-closure outputs. This record is
immutable evidence for the current blocked candidate and must not be edited
into a later promotion decision.
