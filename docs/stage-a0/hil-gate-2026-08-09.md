# Stage A0 bounded HIL gate — 2026-08-09

## Current result

No HIL claim is made. The designated disposable `misterpi` identity is
private and is not present in the repository or generated evidence. The local
private SSH password file exists with the expected owner-only permissions, but
the privately configured target candidate did not answer the bounded SSH
readiness check during this run. No target mutation, reboot, deployment, or
credential disclosure was performed.

This is an external-state blocker, not a software verification failure. The
Stage A classification remains **Software-tested**.

## Required bounded run when the kit is reachable

1. Resolve the exact target through private operator configuration; do not use
   a hostname, IP address, or historical fingerprint as designation proof.
2. Start `scripts/dev-target-tunnel.sh` with the private target settings and
   verify the direct target `/v1/health` response. Require `ready=true`,
   `mister_process=true`, and `command_pipe=true`.
3. Record the target image/software identity and the Stage A binary hashes in
   a private run worksheet; do not put target addresses, tokens, or passwords
   in Git.
4. Run the smallest compatibility slice: launch one known-good content item,
   verify HDMI/video, audio, input, save/load, and clean shutdown, then repeat
   with the Stage A binary. Keep host-emulated and target-owned observations
   separate.
5. Retain a canonical report containing exact artifact/source/container
   identities, machine observations, operator observations, and any inference.

The existing POC6 runbook governs tunnel/session ownership and target cleanup;
this document only defines the Stage A comparison boundary.
